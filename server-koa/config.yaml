server:
  host: 0.0.0.0
  port: 8080
  staticDir: public

db:
  type: sqlite
  path: ./data/app.db
  busyTimeout: 5000
  retry:
    attempts: 3
    baseDelayMs: 50

log:
  level: info

auth:
  mode: simple
